// Cedar + temporal clause on the same escaped-quote literal must agree.
permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when { context.input.document == "a\"b" }
when temporal { context.input.document == "a\"b" };
