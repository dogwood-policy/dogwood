// Cedar + temporal clause on the same newline-escape literal must agree.
permit (
    principal,
    action == Drupe::Action::"Read",
    resource
)
when { context.input.document == "a\nb" }
when temporal { context.input.document == "a\nb" };
