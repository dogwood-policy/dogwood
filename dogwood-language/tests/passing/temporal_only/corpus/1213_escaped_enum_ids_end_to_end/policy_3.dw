permit (
    principal,
    action == Escaped::Action::"AccessUnicode",
    resource
)
when temporal {
    formerly within 1h Escaped::Action::"Check"::request{
        input.mode: Escaped::Mode::"u\u{123}"
    }
};
