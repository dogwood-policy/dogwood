permit (
    principal,
    action in [Escaped::Action::"group\x22id"],
    resource
)
when temporal {
    formerly within 1h (
        Escaped::Action::"Witness"::request{} &&
        context.input.mode == 5
    )
};
